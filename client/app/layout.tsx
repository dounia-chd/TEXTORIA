import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'
import { AuthProvider } from '@/contexts/AuthContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'TEXTORIA - AI Marketing Content Generation Platform',
  description: 'TEXTORIA is an AI-powered platform specialized in generating professional content for LinkedIn, marketing agencies, and digital marketing professionals using GPT-4 and DALL-E 3',
  keywords: ['AI', 'marketing', 'LinkedIn', 'digital marketing', 'content generation', 'GPT-4', 'DALL-E 3', 'marketing agency', 'business communication'],
  authors: [{ name: 'TEXTORIA Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
          <Toaster
            position="top-right"
            toastOptions={{
              duration: 4000,
              style: {
                background: '#363636',
                color: '#fff',
              },
              success: {
                duration: 3000,
                iconTheme: {
                  primary: '#22c55e',
                  secondary: '#fff',
                },
              },
              error: {
                duration: 5000,
                iconTheme: {
                  primary: '#ef4444',
                  secondary: '#fff',
                },
              },
            }}
          />
        </AuthProvider>
      </body>
    </html>
  )
}
